{
  "name": "sentinal-deriv-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deriv digit-contract desk for the synthetic indices — measures the tape, prices the contract, never predicts the next digit.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "build:standalone": "vite build --config vite.standalone.config.ts && node scripts/build-standalone.mjs",
    "publish:local": "npm run build:standalone && cp dist-standalone/index.html index.html && cp dist-standalone/404.html 404.html && cp dist-standalone/manifest.webmanifest manifest.webmanifest && cp dist-standalone/sw.js sw.js && mkdir -p icons && cp dist-standalone/icons/*.png icons/"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.7.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  },
  "engines": { "node": ">=20" }
}
